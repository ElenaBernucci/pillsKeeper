<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.PillsKeeper">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PillsKeeper">
        <activity android:name=".funzioni"></activity>
        <activity android:name=".provaFunzioni" />
        <activity android:name=".MenuIniziale" />
        <activity
            android:name=".pillole"
            android:label="@string/title_activity_farmaci"
            android:theme="@style/Theme.PillsKeeper" />
        <activity android:name=".contatti" />
        <activity android:name=".dottore" />
        <activity android:name=".farmacia" />
        <activity android:name=".reminder" />
        <activity android:name=".menu_iniziale" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>